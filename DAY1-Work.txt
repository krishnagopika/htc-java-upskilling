Q1.Consider an email id in the format firstname.lastname@xyz.com, and write a program to print the first letter of the first name and the first letter of the last name in upper case.

	
import java.util.Scanner;

public class FnLnUpperCase {
	public static void main(String[] args) {
		Scanner sc=new Scanner(System.in);
		String s=sc.nextLine().toUpperCase();
//		String n[]=s.split("@");
//		String n1[]=s.split("\\.");
		String n2[]=s.split("\\.");
		System.out.println(n2[0].charAt(0)+" "+n2[1].charAt(0));
		 
	}
}


Q2.Write a program to find if a matrix is symmetric. Print Yes if the matrix is symmetric, else print No.


import java.util.Scanner;

public class Symmetric {
	public static void main(String[] args) {
		Scanner sc=new Scanner(System.in);	
		int a=sc.nextInt();
		int b=sc.nextInt();
		int m[][]=new int [a][b];
		for(int i=0;i<a;i++) {
			for(int j=0;j<b;j++) {
				m[i][j]=sc.nextInt();
			}
		}
		for(int i=0;i<a;i++) {
			for(int j=0;j<b;j++) {
				System.out.println(m[i][j]+"\t");;
			}
			 System.out.println();
		}
		if(a!=b) {
			System.out.println("not sysmenetr");
		}else {
			boolean s=true;
			for(int i=0;i<a;i++) {
				for(int j=0;j<b;j++) {
					if(m[i][j]!=m[j][i]) {
						s=false;
						break;
					}
				}
			}
			if(s) {
				System.out.println("symmetric");
			}else {
				System.out.println("not");
			}
		}			
	}

}


Q3: Print a given input string in the title case.


import java.util.Scanner;

public class TitleCase {
	 public static void main(String[] args) {
	
			        String s = "welcome to revature";
			        String s1 = "";
			        String[] w = s.split(" ");
			        for (String m : w) {
			            String a = "";
			            String b = String.valueOf(m.charAt(0)).toUpperCase();
			            String c = m.substring(1).toLowerCase();
			            a = b + c;
			            s1 += a + " ";
			        }
			        System.out.println(s1.trim());
			    }
			}

Q4: John and Jacob are playing a dice game, if the number rolled is even, John get's a point., if the number rolled is odd Jacob get's a point. John and Jacob played N games. Print who won at the end of N games. if its a tie print Tie.
Simple
Sample Input: 4 7 2 3 4
Sample Output:
Tie
Sample Input: 4 9 2 3 5
Sample Output:
Jacob
Sample Input: 4 3 2 6 4
Sample Output:
John
ANS

	
import java.util.Scanner;

public class EvenOrOddOnJJ {
	 public static void main(String[] args) {
	        Scanner scanner = new Scanner(System.in);
	        System.out.print("Enter the number of games: ");
	        int N = scanner.nextInt();

	        int johnScore = 0;
	        int jacobScore = 0;

	        for (int i = 1; i <= N; i++) {
	            int diceRoll = scanner.nextInt();
	            if (diceRoll % 2 == 0) {
	                johnScore++;
	            } else {
	                jacobScore++;
	            }
	          
	        }

	        if (johnScore > jacobScore) {
	            System.out.println("John wins!");
	        } else if (jacobScore > johnScore) {
	            System.out.println("Jacob wins!");
	        } else {
	            System.out.println("Tie");
	        }
	   }
}

Q5.Write a program to find the nth smallest element in an array.

	
import java.util.Scanner;

public class NthSmallestNum {

	public static void main(String[] args) {
		Scanner sc=new Scanner(System.in);	
		int size=sc.nextInt();
		int a[]=new int[size];
		
		for(int i=0;i<a.length;i++) {
			a[i]=sc.nextInt();
		}int b=a[0];
		for(int i=0;i<a.length;i++) {
			if(a[i]<b) {
				b=a[i];
			}
		}
		System.out.println(b);
		
	}

}

Q6.A string is sent in an encrypted format, each character should be shifted by a key value to reveal the secret message.
Medium
Input format:
String s
key k
Sample Input:
gneodj]
Sample Output:
krishna
Explanation:
all the characters in sample
input are shifted right by 4


	
public class EncryptedFormat {
	public static String encrypt(String m, int key) {
        StringBuilder encrypted = new StringBuilder();

        for (int i = 0; i < m.length(); i++) {
            char ch = m.charAt(i);
            
            if (Character.isLetter(ch)) {
                char shifted = (char) (((ch - 'A' + key) % 26) + 'A');
                encrypted.append(shifted);
            } else {
                encrypted.append(ch);
            }
        }

        return encrypted.toString();
    }

    public static String decrypt(String em, int key) {
        return encrypt(em, 26 - key); // Decryption is just encryption with the reverse key
    }

    public static void main(String[] args) {
        String message = "HELLO WORLD";
        int key = 3;

        String encrypted = encrypt(message, key);
        System.out.println("Encrypted message: " + encrypted);

        String decrypted = decrypt(encrypted, key);
        System.out.println("Decrypted message: " + decrypted);
    }
}

Q7.  Write a program to print the multiples of the number at the first index in an array.
Simple
Input format:
(Size of the array)S Array elements
Sample Input:
4
2568
Sample Output:
68
Explanation:
The first element is 2. 6 and 8 are multiples of 2.

	
import java.util.ArrayList;
import java.util.Scanner;

public class FirstIndexMultipul {
	public static void main(String[] args) {
		
	        int[] n = {2, 5, 6, 8};
	        ArrayList<Integer> al = new ArrayList<>();
	        
	        for (int i = 0; i < n.length; i++) {
	            if (n[i] % 2 == 0) {
	                al.add(n[i]);
	            }
	        }
	        
	        for (int i : al) {
	            System.out.println(i);
	        }
	    
	}
}

Q8.Write a program to print the perfect squares in the input array.


import java.io.IOException;
import java.util.Scanner;

public class PerfectSqures {
	 public static boolean isPerfectSquare(int num) {
	        int sqrt = (int) Math.sqrt(num);
	        return sqrt * sqrt == num;
	    }

	    public static void main(String[] args) {
	        int[] numbers = { 4, 9, 16, 25, 30, 36, 49, 64 };

	        System.out.println("Perfect squares in the array:");
	        for (int num : numbers) {
	            if (isPerfectSquare(num)) {
	                System.out.println(num);
	            }
	        }
	    }
}

Q9.The XYZ company has N number of employees. If an employee's name is the least common, that name is rare. Find the rare name in XYZ company(print only one name).

Input format:
N
Name1 Name 2 Name 3 ....,Name n
Sample Input:
8
Jake Jane Jake Albert Alex Albert Albert Jane
Sample Output:
Alex


import java.util.HashMap;
import java.util.Map;
import java.util.Scanner;

public class UniqueNames {
	public static void main(String[] args) {
		Scanner sc = new Scanner(System.in);
	
		String a = sc.nextLine();
		String n[]=a.split(" ");
		HashMap<String,Integer> h=new HashMap<>();
		for(String s:n) {
			if(h.containsKey(s)) {
				h.put(s, h.get(s)+1);
			}else {
				h.put(s, 1);
			}
		}
		int count=Integer.MAX_VALUE;
		String uN=" ";
		for(Map.Entry<String, Integer> i:h.entrySet() ) {
			if(count>i.getValue()) {
				count=i.getValue();
				uN=i.getKey();
			}
		}
		System.out.println("Unique Name: "+uN);
	}

}

Q10.The XYZ company has N number of employees, if the name of the employee is in the format of Ana or Hanah, they are special names. Print the count of the special names in the XYZ company.
Medium--
Input format:
N
Name1 Name 2 Name 3 ....,Name n
Sample Input:
4
Bob Sarah Elle Khloe
Sample Output:
2

import java.util.Scanner;

public class SpecialName {
	 public static void main(String[] args) {
	        Scanner scanner = new Scanner(System.in);
	        System.out.print("Enter the number of employees: ");
	        int a = scanner.nextInt();
	        scanner.nextLine();  
	        int count = 0;
	        
	        for (int i = 1; i <= a; i++) {
	            System.out.print("Enter the name of employee " + i + ": ");
	            String n = scanner.nextLine();
	            
	            if (n.equalsIgnoreCase("Ana") || n.equalsIgnoreCase("Hanah")) {
	                count++;
	            }
	        }
	        
	        System.out.println("Number of special names: " + count);
	        
	        scanner.close();
	    }
	}

